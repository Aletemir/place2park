{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    
    {{ form_row(form.dateStart) }}
    
    {{ form_row(form.dateEnd) }}
    {% if  %}
        
    {% endif %}
    <br>
    <button class="btn">Envoyer</button>
    {{ form_end(form) }}



{% endblock %}
