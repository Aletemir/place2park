{% extends 'base.html.twig' %}

{% block body %}
    {{ disponibility.parking.title }}
    {{ disponibility.price }}
    {{ disponibility.dateStart|date('d/m/Y') }}
    {{ disponibility.dateEnd|date('d/m/Y') }}

{% endblock %}

{% block title %}

{% endblock %}

