{% extends 'base.html.twig' %}

{% block body %}
    {{ disponibility.parking.title }}
    {{ disponibility.price }}
    {{ disponibility.dateStart }}
    {{ disponibility.dateEnd }}

{% endblock %}

{% block title %}

{% endblock %}

